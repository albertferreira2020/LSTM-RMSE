# .env - Configurações do banco de dados
# Copie este arquivo e ajuste com suas credenciais

# PostgreSQL Configuration
DB_HOST=ip_address
DB_PORT=5432
DB_NAME=btc
DB_USER=postgres
DB_PASSWORD=

# SSL Configuration (opcional)
DB_SSLMODE=prefer

# Timeout Configuration
DB_CONNECT_TIMEOUT=30
DB_COMMAND_TIMEOUT=60
